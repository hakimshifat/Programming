#include <bits/stdc++.h>
using namespace std;
#define size 10001;
char ara[size];

void solve()
{
    int a,b,root;
    for(int i=2;i<size;i++){
        ara[i] = 1;
    }
    root = sqrt(size);
    for(int i=2;i<=root;i++){
        
    }
}
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t = 0;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}