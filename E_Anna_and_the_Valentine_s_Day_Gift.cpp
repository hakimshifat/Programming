#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n, m ; cin >> n >> m;
    vector<int>li_st;
    int input;
    for(int i = 0; i < n ;i++){
        cin >> input;
        li_st.push_back(input);
    }
}
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t = 0;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}